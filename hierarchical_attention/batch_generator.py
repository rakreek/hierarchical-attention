
from .utils import docs_to_tensor

class BatchGenerator(object):

	def __init__(self, x, y, sent_len=None):
		pass
		# index by doc len
		# compute frequence of each doc len

	def __iter__(self):
		pass
		# randomly sample from 